<%#=
<div>
  <% @students.each do |student| %>
  <%#=
    <div><%= student.to_s %>
  <%#=
</div>
  <% end %>
<%#=
</div>
%>

<h1>Student List</h1>

 <% @students.each do |t| %>

   <p>
     <%= t.first_name + " " + t.last_name%>
   </p>

 <% end %>
